/////////////////////////////////////////////////////////////////////////////////////////////////////////////////       BATCHFRAME.COM//       Copyright 2014 - BatchFrame.com//       All Rights Reserved////////////////////////////////////////////////////////////////////////////////////////////////////////////////* This script will is meant to be placed in the 'Startup' folder of After Effects.** On startup, this script will automatically create a new composition with a single layer* and apply the specified effect. After running, the script will automatically delete itself.**/// create a new comp and layer, then apply the custom controlvar newComp = app.project.items.addComp("New Comp", 1920, 1080,1,10,23.976);var newLayer = newComp.layers.addSolid([49,70,91], "Custom Control Layer", 1920,1080, 1, 10);var newEffect = newLayer.effect.addProperty("ADBE Custom Control Randy");// make the new comp activenewComp.openInViewer();// Check if file system access is available, otherwise alert the user.var networkAccess = app.preferences.getPrefAsLong("Main Pref Section", "Pref_SCRIPTING_FILE_NETWORK_SECURITY");if (networkAccess == 1) {		// delete the file	var theFile = File($.fileName);	theFile.remove()	} else {		// file can't be deleted	alert("The script does not have file system access and cannot delete itself. Please manually delete the script, otherwise it will be run each time After Effects is started.", "No File System Access");	}