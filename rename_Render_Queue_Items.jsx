for(i=1;i<=app.project.renderQueue.items.length;i++){        var om1 = app.project.renderQueue.item(i).outputModule(1);    var file_name = File.decode( om1.file.name );    var fileArray = file_name.split("_");    fileArray.pop();    var newFileName = fileArray.join("_");        var new_data = {    "Output File Info":    {    "Base Path":"~/Desktop/_CUTOUTS",    "Subfolder Path":"",    "File Name": newFileName    }    };    om1.setSettings( new_data );    }